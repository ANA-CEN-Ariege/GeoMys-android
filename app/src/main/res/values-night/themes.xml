<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GeoMys" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/orange</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Pas de android:statusBarColor : déprécié en Android 15 (bord-à-bord), et la barre est
             déjà transparente par défaut avec enableEdgeToEdge (MainActivity). Même fond partout. -->
        <item name="android:windowBackground">@drawable/bg_accueil</item>
    </style>
</resources>