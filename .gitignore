*.iml
.gradle
/local.properties
# IDE : régénéré par Android Studio à l'ouverture du projet (fichiers machine/session :
# device sélectionné, settings IA, etc.) — rien à partager.
/.idea
# Claude Code : settings.local.json = permissions personnelles avec chemins machine.
/.claude
.DS_Store
/build
/captures
# Signature release : credentials et keystores ne doivent JAMAIS entrer dans le dépôt
# (cf. keystore.properties.example pour le gabarit).
keystore.properties
*.jks
*.keystore
.externalNativeBuild
.cxx
local.properties