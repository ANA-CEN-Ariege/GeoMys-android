<?xml version="1.0" encoding="utf-8"?>
<!--
  Ligne de la liste d'autocomplétion d'espèce : le nom choisi, et juste dessous le nom
  scientifique en italique (le nom français en mode « noms scientifiques »). Le second
  TextView est masqué quand il n'y a rien à montrer, la ligne reprend alors la hauteur
  d'une suggestion ordinaire.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/tv_suggestion_nom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="?android:attr/textColorPrimary"
        />

    <TextView
        android:id="@+id/tv_suggestion_secondaire"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textStyle="italic"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="?android:attr/textColorSecondary"
        android:visibility="gone" />
</LinearLayout>
